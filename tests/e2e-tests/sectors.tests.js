describe('Clicking on the login button ', function(){  
  beforeEach(() => {
    browser.get('');
  });

  it('should be able to download a sector', function() {
    //browser.switchTo().frame('toposya_online_view');
    //var searchField = element(by.id('home_page_search'))
    //searchField.sendKeys('test')
  })
});
